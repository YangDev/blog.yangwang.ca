describe('neural nets', function() {
    it('module neural.networks.util should be defined', function() {
        console.log( );
        expect(angular.module('neural.networks.util')).toBeDefined();
    });
    
    it('module neural.networks should be defined', function() {
        console.log( );
        expect(angular.module('neural.networks')).toBeDefined();
    });
});